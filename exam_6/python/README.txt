1. 구구단 출력 (이중 for 구문 / 옆으로 출력/ 거꾸로 출력)

2. 정렬하기 ( csv(엑셀)를 2차원 리스트에 저장하고 정렬하기) -중복제거 없음

3. 두 자연수 사이의 총합 (범위 숫자 합) 

4. 소수 구하기 (1~100까지) / 소수합계 구하기 (리스트 500개 저장)/ 2부터 소수 아닌 수 500개 

5. 2차원 배열 /리스트 (0~255 랜덤, 평균, 최d대/최소, 중위수)

6. 문자열에서 숫자만 추출 (영문자가 뒤/앞에 있을 때 -거꾸로:reversed)

7. csv(엑셀)파일 처리

8. 바이너리 파일 처리

9. 문자열 변환(아스키코드활용)

10. 인덱스 활용 (0의 위치)

11. displayImage(속도개선)

12. 영상처리 1

13. 시각화, 상관관계도, 파일다운

14. 영상처리 회전

15. 영상처리 확대

16. 영상처리 축소

17. 영상이동

18. 영상상하반전

19. 영상 밝게 / 어둡게

20. 영상 반전하기

21. 영상 흑백(128/ 평균/ 중위수 기준)

22. 영상 파라볼라

23. 영상처리2(칼라)

24. <Jupyter환경> 리스트 내포 [ ] , for. for문 if

25. <Jupyter환경: BeautifulSoup 웹크로링 라이브러리> 1. 부분 추출(select)

26. <Jupyter환경>구구단

27. <Jupyter환경>생성자 메서드

28. <Jupyter환경:BeautifulSoup 웹크로링 라이브러리> 2. 부분 추출(람다함수lambda)

29. <Jupyter환경>절대값

30. <Jupyter환경:BeautifulSoup 웹크로링 라이브러리> 3. 태그 속 속성 링크 모두 추출 또 내용 추출

31. <Jupyter환경>enumerate

32. <Jupyter환경:BeautifulSoup 웹크로링 라이브러리> 4. 로그인 홈페이지 개인정보(마일리지/이코인)

33. <Jupyter환경> filter함수 리스트에서 조건 추출하기

34. <Jupyter환경:BeautifulSoup 웹크로링 라이브러리> 영화 제목 링크 리스트 가져오기

35. <Jupyter환경>map(맵핑)

36. <Jupyter환경:BeautifulSoup 웹크로링 라이브러리> 날씨 내용 추출

37. <Jupyter환경>승

38. <Jupyter환경:BeautifulSoup 웹크로링 라이브러리> info = {}

39. <Jupyter환경>zip함수

40. <Jupyter환경:BeautifulSoup 웹크로링 라이브러리>홈페이지 질문 가져오기 (링크 걸기) 

41. <Jupyter환경>lambda함수

42. <Jupyter환경>정규표현식 compile, match, find, [ ] , { } , ?

43. <Jupyter환경> *, +, ., ^, \, 헷갈리는 정규표현식 구분, 매칭 다수( or=| )

44. <Jupyter환경>match, search, findall, span

45.<Jupyter환경>[ㄱ-힝]

46. <Jupyter환경>{} 묶음, 핸드폰 번호

47. <Jupyter환경>match, group

48. <Jupyter환경>group

49. <Jupyter환경>re.findall

50. <Jupyter환경>파일 다운

51. <Jupyter환경>문자열 추출

52. 자연어처리 konlpy.org

53. 스크래핑, 웹데이터 가져와 파일 저장하기

54. 자연어처리 import, 단축키 활용

55. 스크래핑함수 + 파일정제작업

56. 빈도수 묶음으로 파일 저장하기

57. 시각화

58. 한글 깨짐 방지

59. NumPy의 np, shape, 벡터화 연산(배열 원소 각 2배)

60. numpy 배열 벡터화 연산 2

61. numpy 배열 len

62. numpy n차원(asarray, ndim, shape)

63. numpy 다차원 배열 인데싱

64. numpy 배열 슬라이싱

65. numpy 초기화

66. numpy 초기화2 (reshape.ones_like)

67. numpy 단위행렬

68. numpy 불린 참조, 자리바꾸는 방법_인덱싱활용 [ ]

69. numpy 형변환

70. numpy 무한대, nan, 선형공간, 전치행렬

71. numpy arange -> reshape(행크기, 열크기, 깊이크기) -1 : 알아서 계산

72. numpy arange, reshape, flatten

73. numpy newaxis

74. numpy 배열연결 hstack, vstack, dstack, r, concatenate

75. numpy title

76. numpy 난수 random

77. 확률변수와 확률분포

78. numpy seed(난수 동일 설정)

79. numpy 이항분포

80. numpy 정규분포

81. numpy 균등분포

82. numpy 데이터자료형

83. numpy 배열 스칼라 연산

84. numpy 계산 시간 알기

85. numpy in, not in (속하는가 안 속하는가)

86. numpy 선형대수, np.all(조건)

87. numpy e의 승

88. numpy broadcasting

89. numpy 설치 방법

90. numpy element-wise product 요소 간 곱셈, (not) equal, greater, less, copy

91. numpy 난수 소수점 rand/ rands

92. numpy shuffle 뒤썩기, choice 무작위선택(복원추출, 비복원추출)

93. numpy rand, randn, randint

94. numpy 중복제거 unique

95. numpy 길이 bincount

96. 선형대수/ 선형조합/ 내적구하기 

97. numpy 기술통계

98. numpy 대각행렬

99. numpy 사분위, 이상치

100. scipy 전체 기술통계2

101. numpy 전치행렬

102. sklearn.datasets 내적

103. numpy 4차원 표현 

104. 1~10000까지 8이라는 숫자가 총 몇 번 나오는가?

105. numpy copy

106. numpy [],[][],[,,]

107. numpy around(반올림)

108. numpy prod(곱셈)

109. numpy nan 값 제외하고 계산

110. numpy 누적곱셈/ 덧셈 함수

111. numpy sin, cos

112. numpy abs(절대값)-any함수, count함수,modf(정수/실수 나눠 튜플 형태), isnan(nan값 확인), isfinite(유한수 확인), sign(음수, 양수 확인 )

113. numpy reshape(-1)

114. numpy set(집합함수), intersect1d(교집합), union1d(합집합), in1d(요소 속하는 여부), setdiff1d(차집합), 대칭차집합(setxor1d)

115. numpy 최소, 최대, 조건색인 추출 : np.argmin(), argmax(), where()

116. numpy 오름차순, 내림차순

117. numpy 딕셔너리 정렬

118. numpy save

119. numpy 역행렬구하기

120. pandas 데이터프레임 (파일읽기)

121. pandas 데이터프레임

122. warnings 오류제거

123. pandas ix 데이터 추출

124. pandas head, tail 데이터 추출

125. pandas 열기준으로 데이터 추출

126. pandas 시계열분석

127. pandas 데이터 합치기 : concat, join

128. pandas key

129. pandas 딕셔너리로 데이터프레임, reindex

130. pandas 데이터프레임 결합 concat, key

131. pandas 데이터프레임 결합 append

132. pandas merge 함수

133. pandas 이름 중복 (행row)

134. pandas merge(), join()

135. pandas 데이터프레임 열 인덱스와 행 인덱스 혼합병합

136. pandas info

137. pandas 결측값을 각 변수별 평균으로 대체

138. pandas isnull: 결측값

139. pandas isnull().sum()

140. pandas 행과 열에 None값 넣기, sum, cumsum, mean, std, df+df_2

141. pandas fillna

142. pandas 결측값 대체 처리

143. pandas fillna대체

144. pandas  np.where(조건, 참, 거짓)

145. pandas fillna

146. pandas 문자를 날짜형태로 변환

147. pandas duplicated 중복확인

148. 시각화

149. pandas replace, unique, value_counts

150. 분석1

151. 결측값, 결정트리, 랜덤포레스트

152. 지도

153. 0값 대체처리, 예측값, 실제값(로그취함), 모델생성(동일난수)

154. 연습문제

155. 표처리

156. KNN

157. k-meams클러스터 분리(연간소득/소득점수 k=5분리)

158. DecisionTree(유방암)

159. knn(타이타닉 생존율 모델 80%정확도)

160. 버섯 분류기

161. 텐서플로워(딥러닝-일변항)

162. tensorflow 다변량 분석 

163. softmax

164. 동물판정 분류기

165. 데이터 전처리 과정

166. 복잡한 데이터 분석 (AND, OR, XOR)

167. 신경망, 이미지 분류기, 깊이

168. navie_bayes 분류기 (스펨분류기)

169. RNN

170. 자연어처리(중복)

171. 주가예측

172. 자연어처리(인코딩)

173. 자연어처리

174. 자연어처리 (인덱스로 인코딩)

175. 자연어처리(DTM(TF-IDF))

176. keras 케라스

177. keras(단순 선형회귀, 다중 선형회귀) 

# 작성일 : 2022.08.01
# github 코드 정리 : 2026.09.21
